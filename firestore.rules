rules_version = '2';

// Enquiries are written only by the Cloud Function using the Admin SDK, which
// bypasses these rules. No client — browser or app — may read or write anything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
